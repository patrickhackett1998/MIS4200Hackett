@model IEnumerable<MIS4200Hackett.Models.TreatmentLineItem>

@{
    ViewBag.Title = "Index for Treatment Line Item";
}

<h2>Index for Treatment Line Item</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Patient.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Treatment.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.qtyOrdered)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.price)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Patient.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Treatment.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.qtyOrdered)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.price)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.orderdetailID }) |
            @Html.ActionLink("Details", "Details", new { id=item.orderdetailID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.orderdetailID })
        </td>
    </tr>
}

</table>
