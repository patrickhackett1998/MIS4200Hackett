@model IEnumerable<MIS4200Hackett.Models.Treatment>

@{
    ViewBag.Title = "Index for Treatment";
}

<h2>Index for Treatment</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.unitCost)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.unitCost)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TreatmentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TreatmentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TreatmentID })
        </td>
    </tr>
}

</table>
